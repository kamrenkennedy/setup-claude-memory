# Family Memory Changelog

_Last compacted: {{INSTALL_DATE}} — rolling window of 30 most recent entries._
_Older entries: see `changelog-archive/YYYY-MM.md`._

**Write discipline:** append-only. Never delete entries from this file — older entries get archived by month during session wraps. ISO timestamp format `YYYY-MM-DDTHH:MM`. Sign with initials (KK / TK).

---

## {{INSTALL_DATE}}T{{INSTALL_TIME}} — {{INSTALL_USER}}
Family memory initialized. Templates deployed via setup-claude-memory v{{CLI_VERSION}}.
